#include "main.h"

/**
 * _atoi - converts a string to an integer
 * @s: string
 * Return: integer
 */
int _atoi(char *s)
{
	int n, i;

	for (i = 0; s[i] != '\0', i++)
	{
		if (c >= 48 && c <= 57)
			
	
	}
