#include "main.h"

/**
 * main - Prints if a number is +ve or -ve
 * Return: Always 0 success
 */

int main(void)
{
        int i;

        i = 0;
        positive_or_negative(i);

        return (0);
}
